<% if(typeof error != 'undefined'){ %>
    <% error.forEach(function(error){%>
        <div class="bg-red-500 text-white px-4 py-3 text-center m-2 text-lg rounded-lg">
            <%= error.msg %>
        </div>
    <% });%>  
<% } %>